server:
  port: 8003

spring:
  application:
    name: ldx-order
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: happy
    url: jdbc:mysql://127.0.0.1:3307/wfw?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7000/eureka
